# HW01: N-Queens Solver (八皇后问题求解器)

## 实现思路
本项目实现了一个标准的 N 皇后问题求解器。
核心算法采用 **回溯法 (Backtracking)**。通过逐行放置皇后，并检查列和对角线是否冲突。
为了提升性能，算法进行了重构优化：使用 `set` 数据结构来记录已经被占用的列 (`cols`)、主对角线 (`diag1`) 和副对角线 (`diag2`)，将冲突检查的时间复杂度从 $O(N)$ 降低到了 $O(1)$。

## 运行与测试方式

本项目使用 `pytest` 进行自动化测试。请确保你的环境中已安装 `pytest`。
